@model IEnumerable<KucukkoyIHL.Models.Event>

@{
    ViewData["Title"] = "Etkinlik Yönetimi";
}

<div class="hero-section text-center">
    <div class="container">
        <h1><i class="fas fa-calendar-alt"></i> Etkinlik Yönetimi</h1>
        <p class="lead">Tüm etkinlikleri görüntüleyin ve yönetin</p>
    </div>
</div>

<div class="container">
    <div class="mb-4">
        <a href="/Admin/CreateEvent" class="btn btn-success btn-lg">
            <i class="fas fa-plus"></i> Yeni Etkinlik Ekle
        </a>
        <a href="/Admin" class="btn btn-secondary btn-lg">
            <i class="fas fa-arrow-left"></i> Geri Dön
        </a>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table-light">
                        <tr>
                            <th>Etkinlik Adı</th>
                            <th>Tarih</th>
                            <th>Konum</th>
                            <th>Durum</th>
                            <th style="width: 200px;">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            @foreach (var evt in Model)
                            {
                                <tr>
                                    <td><strong>@evt.Title</strong></td>
                                    <td>@evt.EventDate.ToString("dd.MM.yyyy HH:mm")</td>
                                    <td>@evt.Location</td>
                                    <td>
                                        @if (evt.IsActive)
                                        {
                                            <span class="badge bg-success">Aktif</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">Pasif</span>
                                        }
                                    </td>
                                    <td>
                                        <a href="/Admin/EditEvent/@evt.Id" class="btn btn-sm btn-warning">
                                            <i class="fas fa-edit"></i> Düzenle
                                        </a>
                                        <a href="/Admin/DeleteEvent/@evt.Id" class="btn btn-sm btn-danger">
                                            <i class="fas fa-trash"></i> Sil
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="5" class="text-center">
                                    <div class="alert alert-info mb-0">
                                        <i class="fas fa-info-circle"></i> Henüz etkinlik bulunmamaktadır.
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>